# FlixNest Production Environment Variables
# Copy this to .env.production and update with your deployed backend URL

# ============================================================
# BACKEND SCRAPER (PRIMARY - Required for NativePlayer)
# ============================================================

# URL of the deployed Playwright scraper service
# Replace with your actual backend URL after deployment
VITE_SCRAPER_URL=https://vinyl-correspondence-integral-relief.trycloudflare.com/api/extract

# ============================================================
# PROXY WORKER (FALLBACK - for same-origin proxy attempt)
# ============================================================

# URL of deployed Cloudflare Worker proxy
# Production: https://flixnest-proxy.YOUR_SUBDOMAIN.workers.dev
VITE_PROXY_WORKER_URL=http://localhost:8787
